stages:
  - deploy

deploy:
  stage: deploy
  only:
    - prod
  script:
    - rsync -avr ./ $DEPLOY_PATH --exclude '.git'
    - cd $DEPLOY_PATH
    - cnpm install
    - export PM2_HOME=/root/.pm2
    - pm2 start ./bin/www --name mock